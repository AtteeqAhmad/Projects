export default class Quote {
      constructor(data){
            this.body = data.body
            this.author = data.author
      }
      // get Template(data){
      //       return`
      //       "app.controller.quoteController.setActive('${this.url}')">(${this.quote})
      //       )`
      // }
}